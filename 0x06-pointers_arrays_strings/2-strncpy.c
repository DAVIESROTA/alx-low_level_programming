#include "main.h"
char *_strncpy(char *dest, char *src, int n);
int i = 0;
int j = 0;

for(i =0 i; i < n && *(src + 1); i++);
{
	*(dest + i) = *(src + i);
}
for(i < n; i++);
{
	*(dest + i) = '\n';
	return dest;
}
	


